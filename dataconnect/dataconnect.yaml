specVersion: "v1"

# Unique service name for Firebase Data Connect
serviceId: "vederra-scheduler-2-service"

# Region for deployment
location: "us-central1"

# Path to your GraphQL schema
schema:
  source: "./schema"

  # PostgreSQL Data Source Configuration
  datasource:
    postgresql:
      database: "vederra-scheduler-2-database"
      cloudSql:
        instanceId: "vederra-scheduler-2-instance"
      # Validation mode — use STRICT after all schema errors are fixed
      schemaValidation: "COMPATIBLE"

# Path to connector definitions (queries, mutations, subscriptions)
connectorDirs:
  - "./example"
  - "./chat"

